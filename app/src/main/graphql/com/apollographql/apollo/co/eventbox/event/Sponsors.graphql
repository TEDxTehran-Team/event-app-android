query GetEventSponsors($eventID:Int!) {
    sponsorsWithType(event: $eventID) {
        sponsors {
            title
            logo
            link
        }
        type {
            title
        }
    }
}